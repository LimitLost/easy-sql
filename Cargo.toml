[package]
name = "easy-sql"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.8", features = [ "runtime-tokio", "uuid" ] }

[features]
backend = ["sqlx/postgres"]
frontend = ["sqlx/sqlite"]