[package]
name = "sql-build"
version = "0.1.0"
edition = "2024"

[dependencies]
easy-macros = { path = "../../easy-macros", features = ["for-macro"] }
proc-macro2 = { version = "*", features = ["span-locations"] }
regex = "1.11.1"